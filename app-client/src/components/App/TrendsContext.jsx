// Note: Currently unused. 
// This component is to be used if trends panel logic is moved to a context-driven approach instead.
import { createContext } from 'react';

const TrendsContext = createContext(false);

export default TrendsContext;